#### What do we need to do on a regular cadence to look over and organize notes?
 - Look through new / updated notes since last sweep
 - Understand them at a semantic level
 - Look through existing tags or related notes
 - Relate to existing notes
 - Tag with relevant tags
 - Add to relevant areas
 - Disambiguate pieces into different note types
 - Potentially add to hierarchy
 - Summarize and add that to hierarchy
#### How do we update this process with user preferences / reinforcement?
- ??
- Some post training/ reinforcement learning thing?
- an "Add to examples" button which will generate the action as an example we like and add it to prompt, within some context limit
#### How do we display the organizational changes to users?
- Change / review queue for users to see
	- Set of text diffs associated with an action that we can review
	- Some form of bundling changes together?
#### What do we need to do to generate nodes?
- At certain levels of text length (post-processed, after includes), generate a summary node in the hierarchy.
- Summary nodes should have hot links to the relevant sections of the summary
- Should have a set of core tags / concept pages that we create after a certain threshold of pages include those tags, which serve as root nodes for an idea

#### How do we queue up things we definitely want to organize / tag vs casual reviews
- Start an agent session for organization?
	- Generates a miniature changes queue for the operation
- Or have a tag for review button / actual tag which places it in an editable queue for review
	- Additional comments associated with the review should also be available
#### How do we want to organize notes?
 - links to each other
 - Create central nodes / tags / zones?
	 - Have central pages that act as a summary of summaries
		 - Would these form naturally?
	 - 

#### Note types
- Code snippets, flowcharts, assets
- Terminology
- Chapters / information
- Concepts - bring together everything in a long form scenario
- Hierarchy of information (summaries of graphs / extra information)

#### How do we store things like summaries in Obsidian?
- We want them to be editable, but also to not clutter up too much space?
- We have a custom section in any note that has a summary added to it
- Summaries of summaries can be stored in the regular file hierarchy, we can figure out something else if they get too cluttering

